/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["img.freepik.com", "via.placeholder.com", "5.imimg.com"],
  },
};

module.exports = nextConfig;
